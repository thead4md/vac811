# vite-react-ssg declares an optional peer on react-router-dom@^6, but this
# project runs react-router-dom@7 (API-compatible for the data-router surface
# vite-react-ssg uses). Allow the optional-peer mismatch so `npm ci` in CI
# resolves the same tree `npm install` produced locally.
legacy-peer-deps=true
